# 🐺 Statham Quotes App (Docker Edition)

Веб-приложение с мудростями Джейсона Стетхема. Реализовано на **FastAPI** с контейнеризацией через **Docker** и четким разделением фронтенда и бэкенда.

---

## 📁 Структура проекта
Проект организован по стандартам чистой архитектуры:

* **app/** — основной код приложения (FastAPI, статика, шаблоны).
* **data/** — хранилище цитат (текстовая база данных `quotes.txt`).
* **scripts/** — вспомогательные инструменты (парсеры и прочее).
* **Dockerfile** & **docker-compose.yml** — конфигурация контейнеризации.

---

## 🛠 Технологический стек
* **Backend:** Python 3.9+, FastAPI, Uvicorn
* **Frontend:** HTML5, CSS3 (Variables, OKLCH), Vanilla JavaScript
* **DevOps:** Docker, Docker Compose

---

## 🚀 Как запустить

### 1. Предварительные требования
Убедитесь, что у вас установлен [Docker Desktop](https://www.docker.com/products/docker-desktop/) (включает Docker Compose).

### 2. Запуск через Docker (Рекомендуется)
Выполните команду в корневой директории проекта:

```bash
docker-compose up --build
```

После запуска приложение будет доступно по адресу:
👉 **[http://localhost:8000](http://localhost:8000)**

### 3. Запуск локально (для разработки)
Если вы хотите запустить проект напрямую:

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   
2. **Запустите сервер:**
   ```bash
   uvicorn app.main:app --reload

# 📚 Quote Generator

## 💾 Сохранение данных

Приложение использует **Docker Volumes**. Это гарантирует, что все цитаты, добавленные вами через интерфейс, сохраняются в локальном файле `./data/quotes.txt`. Данные не исчезнут даже после остановки или удаления контейнера.

## 📝 API Endpoints

- **GET /** — Главная страница.
  
- **GET /quote** — Получить случайную цитату.
  
- **POST /add_quote** — Добавить свою цитату 
  - Принимает JSON:
    ```json
    {
      "quote": "текст"
    }
    ```

> "Главное — не то, как ты написал код, а то, как он запустился в докере." — (c) Джейсон Стетхем
  